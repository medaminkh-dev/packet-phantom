# Packet Phantom v2.0.0 - Core Dependencies
# Network reconnaissance and OS fingerprinting framework
# Python 3.8+

# Core Network & Packet Crafting
scapy>=2.4.5          # Battle-tested packet crafting and parsing (CRITICAL)
                      # Handles platform differences across Windows, macOS, Linux

# Data & Configuration
jsonschema>=4.0.0     # JSON Schema validation for signature database
                      # Ensures signature integrity and schema compliance

# HTTP & Remote Operations (for future CDN support)
requests>=2.28.0      # HTTP library for future CDN signature fetching
                      # Planned for v2.1 hybrid architecture

# Output & Formatting
colorama>=0.4.4       # Cross-platform colored terminal output
                      # Works on Windows, macOS, Linux terminals

# Optional: Development & Testing
# Uncomment these if developing or running tests
# pytest>=7.0.0       # Testing framework
# black>=22.0.0       # Code formatter
# pylint>=2.14.0      # Code linter
# mypy>=0.950         # Static type checker
# sphinx>=5.0.0       # Documentation generator

# Development notes:
# - All core dependencies are pinned to ensure reproducibility
# - Scapy is the ONLY raw socket library - migrated from manual implementation
#   in v2.0 for reliability and maintainability (see Challenges.md)
# - jsonschema enables community-driven JSON signature database
# - requests prepares for CDN distribution in v2.1
